export function selectionSort(arr: number[]): void {
    // Implementação futura
}

function findMinIndex(arr: number[], startIndex: number): number {
    return startIndex; // Retorno temporário para satisfazer o linter
} 